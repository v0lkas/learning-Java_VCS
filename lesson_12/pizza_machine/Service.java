package vcs.lesson_12.pizza_machine;

public class Service {
//    private static final int MAX_USAGE = 20;
//
//    private static final int MIN_SIZE = 20;
//    private static final int MAX_SIZE = 100;
//
//    public static double calculate_size(double size) {
//        if(size >= MIN_SIZE && size <= MAX_SIZE) {
//            double area_min = Math.pow((MIN_SIZE/2-1),2) * Math.PI;
//            double area_new = Math.pow((size/2-1),2) * Math.PI;
//
//            double coefficient = 1 / area_min * area_new;
//
//            return coefficient;
//        }
//        return 0;
//    }
}
